-------------------------------------------------
# 기본 :PROMPT_TEMPLATE = 'You are a web security expert. Classify each HTTP request as "Normal" or "Anomalous" and provide a brief reason.\n\nExamples:\nRequest: GET /index.jsp HTTP/1.1\nOutput: {{"label": "Normal", "reason": "Standard page request, no suspicious pattern"}}\n\nRequest: GET /search?q=\' OR \'1\'=\'1 HTTP/1.1\nOutput: {{"label": "Anomalous", "reason": "Classic SQL Injection pattern with OR 1=1"}}\n\nNow classify:\nRequest: {http_text}\nOutput:'
----------------------------------------------------------
입력: GET /tienda1/publico/anadir.jsp?id=2'+OR+'1'='1 HTTP/1.1
응답: {'label': 'Anomalous', 'reason': "SQL Injection attempt. The request includes ' OR '1'='1', a common SQL injection pattern to bypass authentication or retrieve all data."}
10/100건 완료 (7.8초, 건당 0.78초)
  20/100건 완료 (13.9초, 건당 0.70초)
  30/100건 완료 (20.5초, 건당 0.68초)
  40/100건 완료 (28.3초, 건당 0.71초)
  50/100건 완료 (36.2초, 건당 0.72초)
  60/100건 완료 (44.4초, 건당 0.74초)
  70/100건 완료 (50.5초, 건당 0.72초)
  80/100건 완료 (58.5초, 건당 0.73초)
  90/100건 완료 (65.7초, 건당 0.73초)
  100/100건 완료 (72.4초, 건당 0.72초)

총 소요: 72.4초
1만 건 환산: 약 121분
LLM 정확도: 0.8300
LLM F1:    0.8211
분류 실패(Unknown): 1건

              precision    recall  f1-score   support

      Normal       0.90      0.79      0.84        56
   Anomalous       0.76      0.89      0.82        44

    accuracy                           0.83       100
   macro avg       0.83      0.84      0.83       100
weighted avg       0.84      0.83      0.83       100
---------------------------------------------------------
# 변경후 : SYSTEM_PROMPT = """
You are a professional WAF (Web Application Firewall) analyst.

Your task:
Classify HTTP requests as:
- "Normal"
- "Anomalous"

Detect attacks including:
- SQL Injection
- Cross Site Scripting (XSS)
- Command Injection
- Path Traversal
- Remote Code Execution
- Encoded attack payloads
- Sensitive file access
- Suspicious authentication attempts

Guidelines:
- Consider URL, parameters, body, and encoding
- If there is a clear attack signature -> Anomalous
- If the request looks like normal browsing/API traffic -> Normal
- If unsure, prefer Normal

Return ONLY valid JSON.

Format:
{"label":"Normal|Anomalous","reason":"short explanation"}
"""
============================================================
INPUT:
GET /index.jsp HTTP/1.1

OUTPUT:
{'label': 'Normal',
 'reason': 'Standard webpage request'}

============================================================
INPUT:
GET /search?q=' OR '1'='1 HTTP/1.1

OUTPUT:
{'label': 'Anomalous',
 'reason': 'Rule-based SQL injection detection'}

============================================================
INPUT:
GET /../../etc/passwd HTTP/1.1

OUTPUT:
{'label': 'Anomalous',
 'reason': 'Rule-based path traversal detection'}

============================================================
INPUT:
GET /search?q=<script>alert(1)</script> HTTP/1.1

OUTPUT:
{'label': 'Anomalous',
 'reason': 'Rule-based XSS detection'}

============================================================
INPUT:
POST /api/login HTTP/1.1
Body:{"id":"john"}

OUTPUT:
{'label': 'Normal',
 'reason': 'Typical API login request'}

  10/100건 완료 (18.4초, 건당 1.84초)
  20/100건 완료 (36.7초, 건당 1.83초)
  30/100건 완료 (54.9초, 건당 1.83초)
  40/100건 완료 (73.0초, 건당 1.82초)
  50/100건 완료 (91.5초, 건당 1.83초)
  60/100건 완료 (109.8초, 건당 1.83초)
  70/100건 완료 (128.1초, 건당 1.83초)
  80/100건 완료 (146.4초, 건당 1.83초)
  90/100건 완료 (164.7초, 건당 1.83초)
  100/100건 완료 (183.1초, 건당 1.83초)

총 소요: 183.1초
1만 건 환산: 약 305분
---------------------------------------------------------------
              precision    recall  f1-score   support

      Normal       0.93      0.96      0.94        50
  Anomalous       0.95      0.92      0.94        50

    accuracy                           0.94       100
   macro avg       0.94      0.94      0.94       100
weighted avg       0.94      0.94      0.94       100
------------------------------------------------------------------
